# Telegram bot credentials — fill in real values in a local .env (gitignored).
# Get the token from @BotFather. Get your chat_id by messaging your bot then
# visiting https://api.telegram.org/bot<TOKEN>/getUpdates and reading chat.id.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
